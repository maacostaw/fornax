# ---- Servidor ----
server.port=8080

# ---- Base de datos PostgreSQL ----
# DB_URL y DB_USER se pueden sobreescribir con variables de entorno; la contraseña va fija.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/fornax}
spring.datasource.username=${DB_USER:fornax_user}
spring.datasource.password=${DB_PASSWORD:2gKZ7x1TiN0>}
spring.datasource.driver-class-name=org.postgresql.Driver

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
